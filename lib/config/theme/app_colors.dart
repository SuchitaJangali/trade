import 'package:flutter/material.dart';

class AppColors {
  // Primary
  static const Color lightPrimary = Color.fromARGB(255, 13, 21, 18);

  // Backgrounds
  static const Color lightBackground = Color(0xFFF9F9F9);
  static const Color lightSurface = Color(0xFFFFFFFF);
  static const Color lightCard = Color(0xFFF0F0F0);

  // Text
  static const Color lightText = Color(0xFF1A1A1A);
  static const Color lightTextMuted = Color(0xFF6B6B6B);

  // Semantic
  static const Color gain = Color(0xFF1B7A4E);
  static const Color loss = Color(0xFFCC0000);
  static const Color neutral = Color(0xFF888888);
  static const Color divider = Color(0xFFEEEEEE);
}
